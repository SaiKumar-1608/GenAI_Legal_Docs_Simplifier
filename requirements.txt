# Web framework (if building Python backend/microservice)
fastapi==0.110.0
uvicorn==0.29.0

# LLM / RAG tooling
openai==1.40.0
langchain==0.2.7
llama-index==0.10.40
faiss-cpu==1.8.0

# Data processing
numpy==1.26.4
pandas==2.2.2

# Validation & schema (for MCP bundle JSONs)
pydantic==2.8.2

# Testing & utilities
pytest==8.2.2
requests==2.32.3
httpx==0.27.0

# Optional (OCR / PDF handling, useful for legal docs)
pdfminer.six==20231228
pytesseract==0.3.10

# Optional (monitoring / ops)
prometheus-client==0.20.0
